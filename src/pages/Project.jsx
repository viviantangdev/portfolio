import SectionTitle from '../components/SectionTitle';

const Project = () => {
  return (
    <section>
      <SectionTitle title={'Projects'} />
    </section>

    /*TODO Add prjects*/
  );
};

export default Project;
